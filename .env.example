VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key

# 本地 `npm run dev` 未设置时默认已是 /nestjs-reading/（见 vite.config.ts）
# 若要在 http://localhost:5174/ 根路径开发，取消下一行注释：
# VITE_PUBLIC_BASE_PATH=/

# 显式指定子路径（与默认相同时可省略）
# VITE_PUBLIC_BASE_PATH=/nestjs-reading/

# 默认走 Vite 代理 /api → http://127.0.0.1:3000，可不填
# 线上 / Vercel：填「网关前缀到 /api 之前」的 origin，不要带 /api 后缀
# 例：健康检查为 https://api.fangdu.chat/nestjs/api/health 时：
# VITE_API_BASE=https://api.fangdu.chat/nestjs
# VITE_API_BASE=http://localhost:3000
